{:paths ["src"]
 ;; langgraph brings langchain transitively (both zero-/single-dep,
 ;; all .cljc -> portable to JVM / SCI / ClojureScript / GraalVM). It lives
 ;; in the kotoba-lang org of this workspace (transferred + renamed from
 ;; com-junkawasaki/langgraph-clj / com-junkawasaki/langchain-clj).
 ;;
 ;; Like the fuel-wholesale (`cloud-itonami-isic-4671`), general-trading
 ;; (`cloud-itonami-isic-4690`), commission-brokerage
 ;; (`cloud-itonami-isic-4610`) and agri-wholesale (`cloud-itonami-isic-4620`)
 ;; siblings, this food/beverage/tobacco wholesale vertical is
 ;; SELF-CONTAINED: there is no `kotoba-lang/provisiontrade` to delegate
 ;; food-safety / alcohol-excise / tobacco-excise validation to, so those
 ;; checks live as direct entity boolean reads in `provisiontrade.governor`
 ;; rather than wrapping an external lib. This repo is standalone (forkable
 ;; outside the workspace); the :local/root paths only resolve inside the
 ;; monorepo checkout -- a standalone fork should override them with git
 ;; coordinates.
 :deps {io.github.kotoba-lang/langgraph
        {:local/root "../../kotoba-lang/langgraph"}}
 :aliases
 {;; offline workspace dev -- pin transitive langchain to local checkout
  :dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  ;; clojure -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "provisiontrade.sim"]}
  ;; clojure -M:dev:render-html [out-file] -> regenerate docs/samples/operator-console.html
  ;; through the real actor (flagship checklist item 2, ADR-2607189300 Wave5).
  :render-html {:main-opts ["-m" "provisiontrade.render-html"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
